"""Multi-site crawl configuration.

Import ``SITES`` from ``.sites`` to get the full site list.
To add a new site, append a dict to ``SITES`` in ``sites.py``.
"""
from .sites import SITES, CRAWL_SETTINGS
